import React from 'react';
import MenuList from '../components/MenuList';

const Home: React.FC = () => {
    return (
        <div>
            <h1>Welcome to the Home page!</h1>
            <MenuList />
        </div>
    );
};

export default Home;